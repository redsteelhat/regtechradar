// Stripe SDK wrapper — Checkout, Customer Portal
// import Stripe from 'stripe';
// export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, { apiVersion: '2024-xx' });
export const stripe = null;
