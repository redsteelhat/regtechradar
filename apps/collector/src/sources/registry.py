"""Load source definitions from YAML registry."""
from pathlib import Path
from typing import Any

# TODO: add PyYAML and load sources/registry.yaml
# def load_registry(path: Path | None = None) -> list[dict[str, Any]]:
#     ...
def load_registry(path: Path | None = None) -> list[dict[str, Any]]:
    """Load source registry. Returns list of source config dicts."""
    return []
