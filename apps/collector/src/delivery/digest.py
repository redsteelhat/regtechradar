"""Weekly digest builder — content selection, intro text, stats."""
# TODO: build digest payload for Resend + React Email
def build_weekly_digest(week_start: str, week_end: str) -> dict:
    """Build digest payload."""
    return {}
