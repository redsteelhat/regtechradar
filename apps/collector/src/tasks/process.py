"""AI processing pipeline task — parse → summarize → store."""
# TODO: chain after crawl, process raw_content_id
def process_raw_content(raw_content_id: str) -> None:
    """Process a single raw content through AI pipeline."""
    pass
