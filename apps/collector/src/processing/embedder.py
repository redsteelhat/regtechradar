"""Vector embedding generation — Voyage/OpenAI, pgvector (V2)."""
# TODO: EMBEDDING_PROVIDER, store in regulatory_updates.embedding
async def embed(text: str) -> list[float]:
    """Return embedding vector."""
    return []
