"""Impact scoring engine — profile vs update (see regtech.md 7.2)."""
# TODO: IMPACT_SYSTEM_PROMPT, score 0-100, category, reasoning
async def assess_impact(update: dict, profile: dict) -> dict:
    """Return impact_assessment dict."""
    return {}
